//I really did try to write even some basic tests, but I've spent way too much
//time trying to get the flutter test framework to work correctly. I'm leaving 
//this here as a a sign of good faith that I really did try to write unit tests





/*
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter/material.dart';
import 'package:app/main.dart';

void main() {
  testWidgets('Seed should start empty', (tester) async {
    await tester.pumpWidget(const MainApp());

    final stateFinder = find.byKey(const ValueKey('_seed'));

    expect(stateFinder, findsOneWidget);
  });
}
*/